/**
 * Development Configuration
 * 
 * This file contains flags for development mode features.
 * Set DEV_MODE_NO_AUTH to false when ready to re-enable authentication.
 */

export const DEV_MODE_NO_AUTH = true;

